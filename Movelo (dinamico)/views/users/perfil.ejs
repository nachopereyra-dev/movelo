<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/stylesheets/perfil.css">
  </head>
  <body>
    <%- include('../partials/header') %>   <!-----------HEADER----------->

    <main>
      <div class="principal">

        <div class="titulo"><h1>Hola <%= user.name %></h1></div>

        <div class="user-info">
        <div class="imagen"><img src="/images/avatars/<%= user.avatar ? user.avatar : 'default.png' %>" width="180" style="border-radius:100%;" alt="">
            <a class="boton-editar" href="">Editar foto</a>
        </div>

        <div class="edit-info">
          <label for="">Nombre completo</label>
          <p><%= user.name %></p>
          <label for="">Correo electrónico</label>
          <p><%= user.email %></p>
          <label for="">Fecha de nacimiento</label>
          <p><%= user.fecha %></p>
          <label for="">Tipo de usuario</label>
          <p><%= user.usuarioTipo %></p>
        </div>
      </div>
    </div>
    </main>

<%- include('../partials/footer') %>   <!-----------FOOTER----------->
  </body>
</html>
